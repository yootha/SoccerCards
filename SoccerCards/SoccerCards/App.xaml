<?xml version="1.0" encoding="utf-8" ?>
<prism:PrismApplication xmlns="http://xamarin.com/schemas/2014/forms"
                        xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
                        xmlns:prism="clr-namespace:Prism.Unity;assembly=Prism.Unity.Forms"
                        x:Class="SoccerCards.App">

  <Application.Resources>
    <ResourceDictionary>
      <ControlTemplate x:Key="MenuTemplate">
        <Grid>
          <Grid.RowDefinitions>
            <RowDefinition Height="0.2*" />
            <RowDefinition Height="0.7*" />
            <RowDefinition Height="0.1*" />
          </Grid.RowDefinitions>
          <Grid.ColumnDefinitions>
            <ColumnDefinition Width="0.05*" />
            <ColumnDefinition Width="0.95*" />
          </Grid.ColumnDefinitions>
          <BoxView Grid.ColumnSpan="2" />
          <Label Grid.Column="1" Text="{TemplateBinding Parent.HeaderText}" VerticalOptions="Center" />
          <ContentPresenter Grid.Row="1" Grid.ColumnSpan="2" />
          <BoxView Grid.Row="2" Grid.ColumnSpan="2" />
          <Label Grid.Row="2" Grid.Column="1" Text="{TemplateBinding Parent.FooterText}" VerticalOptions="Center" />
        </Grid>
      </ControlTemplate>
    </ResourceDictionary>
  </Application.Resources>  
    
</prism:PrismApplication>